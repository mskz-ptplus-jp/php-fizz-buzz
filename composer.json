{
    "require": {
        "php": "^8.3"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.1.1",
        "phpstan/phpstan": "^1.10.42"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app"
        }
    },
    "scripts": {
        "phpunit": "phpunit",
        "phpstan": [
            "phpstan analyze"
        ],
        "test": [
            "phpstan analyze",
            "phpunit"
        ]
    }
}